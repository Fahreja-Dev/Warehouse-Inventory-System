#pragma once
#include <string>
#include "../../lib/struct_mapping/include/struct_mapping/struct_mapping.h"

struct Barang{
    std::string kode;
    std::string nama;
    double harga;
    int stok;
};